---
title: Clean
description: Remove generated artifacts
---

## SYNOPSIS
`cargo buckal clean`


## DESCRIPTION
Remove artifacts from the `buck-out` directory that buck2 has generated in the past.

This command is equivalent to running `buck2 clean` manually, so it also kills the buck2 daemon.